/**
 * print_sign - check the description.
 * @n: input
 * Description: return sign
 * Return: Always 0.
 */

int print_sign(int n)
{
	if (n > 0)
	return (1);

	if (n == 0)
	return (0);

	if (n < 0)
	return (-1);
}
