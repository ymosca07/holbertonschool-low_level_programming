
/**
 * _islower - Check desc.
 * @c: input
 * Description: Return 1 if lowercase
 * Return: 0 (otherwise) and 1 (lowercase)
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	return (1);

	return (0);
}
